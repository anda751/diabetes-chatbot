{
  "name": "diabetes-care-ai",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "npm install --prefix forntend && npm --prefix forntend run build",
    "dev:backend": "npm --prefix backend run dev",
    "dev:frontend": "npm --prefix forntend run dev"
  },
  "dependencies": {
    "@google/genai": "^1.45.0",
    "cors": "^2.8.6",
    "dotenv": "^17.3.1",
    "express": "^5.2.1",
    "pg": "^8.16.3"
  }
}
