{
  "name": "diabetes-care-ai",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "npm --prefix forntend run build",
    "build:app": "npm run build && npx cap sync android",
    "android:add": "npx cap add android",
    "android:open": "npx cap open android",
    "dev:backend": "npm --prefix backend run dev",
    "dev:frontend": "npm --prefix forntend run dev"
  },
  "dependencies": {
    "@capacitor/android": "^8.3.4",
    "@capacitor/cli": "^8.3.4",
    "@capacitor/core": "^8.3.4",
    "@capacitor/local-notifications": "^8.2.0",
    "@google/genai": "^1.45.0",
    "cors": "^2.8.6",
    "dotenv": "^17.3.1",
    "express": "^5.2.1",
    "pg": "^8.16.3"
  },
  "devDependencies": {
    "typescript": "^6.0.3"
  }
}
